DATABASE SECTION
----------------------------------------------------
CREATE DATABASE mydatabase;
use mydatabase;
CREATE TABLE todos (
    ->     id INT AUTO_INCREMENT PRIMARY KEY,
    ->     title VARCHAR(255) NOT NULL,
    ->     completed BOOLEAN DEFAULT FALSE
    -> );
-----------------------------------------------


DOCKER FILE
-------------------------------------------------------
docker build -t todolist .
docker run -it -p 5000:5000 --env-file .env todolist sh
--------------------------------------------------------